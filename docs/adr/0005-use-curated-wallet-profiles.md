# Use curated wallet profiles

Clients select a supported Wallet Profile and may select an Account Index, which defaults to zero, instead of supplying a raw derivation path. Each profile fixes the derivation method, path template, address format, and signature format; Bitcoin profiles also require an explicit address type and network, with no generic Bitcoin default.
