diff --git a/dist/introspect-DvHp2a64.mjs b/dist/introspect-DvHp2a64.mjs
index c901856de2f587793e31d77c8825e77f7adfaae0..7898f81a273008fd3c492624792a40d7ced661b1 100644
--- a/dist/introspect-DvHp2a64.mjs
+++ b/dist/introspect-DvHp2a64.mjs
@@ -240,7 +240,9 @@ async function resolveAccessTokenClaims(input) {
 		scopes,
 		resources,
 		referenceId,
-		metadata
+		metadata,
+		subject: user ? await resolveSubjectIdentifier(user.id, client, opts) : void 0,
+		clientId: client.clientId
 	}) : {};
 	return stripReservedClaims({
 		...extensionClaims,
@@ -1201,7 +1203,9 @@ async function userInfoEndpoint(ctx, opts) {
 		user,
 		scopes,
 		jwt,
-		requestedClaims
+		requestedClaims,
+		subject: baseUserClaims.sub,
+		clientId: typeof clientId === "string" ? clientId : void 0
 	}) : {};
 	return {
 		...baseUserClaims,
@@ -1352,7 +1356,9 @@ async function createIdToken(ctx, opts, user, client, scopes, nonce, sessionId,
 	const customClaims = stripReservedIdTokenClaims(opts.customIdTokenClaims ? await opts.customIdTokenClaims({
 		user,
 		scopes,
-		metadata: parseClientMetadata(client.metadata)
+		metadata: parseClientMetadata(client.metadata),
+		subject: resolvedSub,
+		clientId: client.clientId
 	}) : void 0);
 	const jwtPluginOptions = opts.disableJwtPlugin ? void 0 : getJwtPlugin(ctx.context).options;
 	const resolvedKey = !opts.disableJwtPlugin && !jwtPluginOptions?.jwt?.sign ? await resolveSigningKey(ctx, jwtPluginOptions) : void 0;
diff --git a/dist/oauth-ScTJEcFV.d.mts b/dist/oauth-ScTJEcFV.d.mts
index e0891c523c2e1b9ca7425b49f8086b0a0fe054e9..794a98414906e0eef8a26b1c79bae4babd172bbf 100644
--- a/dist/oauth-ScTJEcFV.d.mts
+++ b/dist/oauth-ScTJEcFV.d.mts
@@ -1572,6 +1572,10 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
      * authorization request parameter.
      */
     requestedClaims: string[];
+    /** The subject identifier resolved for this user and client. */
+    subject: string;
+    /** The client receiving these claims, when available. */
+    clientId?: string;
   }) => Awaitable<Record<string, any>>;
   /**
    * Custom claims attached to OIDC id tokens.
@@ -1592,6 +1596,10 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
     /** The user object if token is associated to a user. */user: User & Record<string, unknown>; /** Scopes granted for this token */
     scopes: Scopes; /** oAuthClient metadata */
     metadata?: Record<string, any>;
+    /** The subject identifier resolved for this user and client. */
+    subject: string;
+    /** The client receiving the ID token. */
+    clientId: string;
   }) => Awaitable<Record<string, any>>;
   /**
    * Custom claims attached to access tokens.
@@ -1612,6 +1620,10 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
     scopes: Scopes; /** The resources requested. */
     resources?: string[]; /** oAuthClient metadata */
     metadata?: Record<string, any>;
+    /** The resolved user subject, or undefined for non-user grants. */
+    subject?: string;
+    /** The client receiving the access token. */
+    clientId: string;
   }) => Awaitable<Record<string, any>>;
   /**
    * Custom fields to include in the token response body.
@@ -1833,6 +1845,14 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
       max: number;
     } | false;
   };
+  /**
+   * Override subject identifier resolution for every user/client pair.
+   * This takes precedence over the built-in public and pairwise strategies.
+   */
+  resolveSubjectIdentifier?: (input: {
+    userId: string;
+    clientId: string;
+  }) => Awaitable<string>;
   /**
    * Secret used to compute pairwise subject identifiers (HMAC-SHA256).
    * When set, clients with `subject_type: "pairwise"` receive unique,
diff --git a/dist/utils-DO8lmoDw.mjs b/dist/utils-DO8lmoDw.mjs
index 33e84521dbbe85a6c0f4a245adc356dde1f2c9ff..64b9174eb39a1ca2d09ad9d9653f855f2b209e65 100644
--- a/dist/utils-DO8lmoDw.mjs
+++ b/dist/utils-DO8lmoDw.mjs
@@ -698,6 +698,10 @@ async function computePairwiseSub(userId, client, secret) {
 * @internal
 */
 async function resolveSubjectIdentifier(userId, client, opts) {
+	if (opts.resolveSubjectIdentifier) return opts.resolveSubjectIdentifier({
+		userId,
+		clientId: client.clientId
+	});
 	if (client.subjectType === "pairwise" && opts.pairwiseSecret) return computePairwiseSub(userId, client, opts.pairwiseSecret);
 	return userId;
 }
