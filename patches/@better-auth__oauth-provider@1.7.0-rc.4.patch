diff --git a/dist/index.mjs b/dist/index.mjs
index 4d9d13deeddca3760be6d505d26d40570c37d4e2..bec73df85ed36471b99558b4f4b7f4b2f8557f90 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -709,8 +709,8 @@ async function userInfoEndpoint(ctx, opts) {
 	});
 	const baseUserClaims = userNormalClaims(user, scopes ?? [], requestedClaims);
 	const clientId = jwt.client_id ?? jwt.azp;
-	const client = clientId && (opts.pairwiseSecret || hasUserInfoClaimExtension(opts)) ? await getClient(ctx, opts, clientId) : void 0;
-	if (opts.pairwiseSecret && client) baseUserClaims.sub = await resolveSubjectIdentifier(user.id, client, opts);
+	const client = clientId && (opts.pairwiseSecret || opts.resolveSubjectIdentifier || hasUserInfoClaimExtension(opts)) ? await getClient(ctx, opts, clientId) : void 0;
+	if ((opts.pairwiseSecret || opts.resolveSubjectIdentifier) && client) baseUserClaims.sub = await resolveSubjectIdentifier(user.id, client, opts, "userinfo");
 	const extensionUserClaims = scopes?.length ? await collectExtensionUserInfoClaims(opts, {
 		ctx,
 		opts,
@@ -870,7 +870,7 @@ async function computeOidcHash(token, signingAlg) {
 async function createIdToken(ctx, opts, user, client, scopes, nonce, sessionId, authTime, accessToken, extraClaims) {
 	const iat = Math.floor(Date.now() / 1e3);
 	const exp = iat + (opts.idTokenExpiresIn ?? 36e3);
-	const resolvedSub = await resolveSubjectIdentifier(user.id, client, opts);
+	const resolvedSub = await resolveSubjectIdentifier(user.id, client, opts, "id_token");
 	const authTimeSec = authTime != null ? Math.floor(authTime.getTime() / 1e3) : void 0;
 	const customClaims = stripReservedIdTokenClaims(opts.customIdTokenClaims ? await opts.customIdTokenClaims({
 		user,
@@ -1965,17 +1965,8 @@ async function requireActiveAccessTokenWithClaims(ctx, opts, token, clientId) {
 * Applied at the presentation layer so internal validation functions
 * keep real user.id (needed for user lookup in /userinfo).
 */
-async function resolveIntrospectionSub(ctx, opts, payload, introspectingClient) {
-	if (!payload.active || !payload.sub) return payload;
-	const issuerClientId = payload.client_id ?? payload.azp;
-	if (!issuerClientId) return payload;
-	const issuingClient = issuerClientId === introspectingClient.clientId ? introspectingClient : await getClient(ctx, opts, issuerClientId);
-	if (!issuingClient) return payload;
-	const resolvedSub = await resolveSubjectIdentifier(payload.sub, issuingClient, opts);
-	return {
-		...payload,
-		sub: resolvedSub
-	};
+async function resolveIntrospectionSub(_ctx, _opts, payload, _introspectingClient) {
+	return payload;
 }
 async function introspectEndpoint(ctx, opts) {
 	let { token, token_type_hint } = ctx.body;
@@ -2116,7 +2107,7 @@ async function prepareBackchannelLogoutPlan(ctx, opts, input) {
 				try {
 					return {
 						client,
-						sub: await resolveSubjectIdentifier(userId, client, opts)
+						sub: await resolveSubjectIdentifier(userId, client, opts, "logout_token")
 					};
 				} catch (error) {
 					logger.warn(`back-channel logout: unable to resolve subject for client ${client.clientId}`, error);
diff --git a/dist/oauth-Bi2PA_d1.d.mts b/dist/oauth-Bi2PA_d1.d.mts
index e3ee051e8bf87b55b4927189246e5e3bcb9aa231..c72547bc7c6d05cf80f2bd644c786239ff52eb22 100644
--- a/dist/oauth-Bi2PA_d1.d.mts
+++ b/dist/oauth-Bi2PA_d1.d.mts
@@ -1943,6 +1943,18 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
    * @see https://openid.net/specs/openid-connect-core-1_0.html#PairwiseAlg
    */
   pairwiseSecret?: string;
+  /**
+   * Resolve an OIDC-facing subject after the built-in public or pairwise
+   * default has been computed. Access-token and introspection subjects do not
+   * use this hook.
+   */
+  resolveSubjectIdentifier?: (input: {
+    userId: string;
+    clientId: string;
+    subjectType: "public" | "pairwise" | undefined;
+    use: "id_token" | "userinfo" | "logout_token";
+    defaultSubject: string;
+  }) => Awaitable<string>;
   /**
    * Resolves a `request_uri` at the authorize endpoint (PAR support).
    *
diff --git a/dist/register-BotzQoS8.mjs b/dist/register-BotzQoS8.mjs
index 7b650bccda938b8c8a4cf7214f60250a109f9144..feaff8c4baa9b9bb306c57dd84d75e301a512b3b 100644
--- a/dist/register-BotzQoS8.mjs
+++ b/dist/register-BotzQoS8.mjs
@@ -1013,6 +1013,10 @@ async function registerEndpoint(ctx, opts) {
 		...NO_STORE_HEADERS
 	});
 	if (!session && !isTokenAuthorized) {
+		if (body.token_endpoint_auth_method !== "none") throw new APIError("BAD_REQUEST", {
+			error: "invalid_client_metadata",
+			error_description: "unauthenticated registration requires token_endpoint_auth_method \"none\""
+		});
 		if (body.grant_types?.includes("client_credentials")) throw new APIError("BAD_REQUEST", {
 			error: "invalid_client_metadata",
 			error_description: "client_credentials grant requires authenticated registration"
diff --git a/dist/utils-GbnW6qPl.mjs b/dist/utils-GbnW6qPl.mjs
index 8e7c75ff47e66669950049d947f850291b6db5e5..6a29ceb0e24dcd5c542fe0436e444aeee566cbb3 100644
--- a/dist/utils-GbnW6qPl.mjs
+++ b/dist/utils-GbnW6qPl.mjs
@@ -725,9 +725,16 @@ async function computePairwiseSub(userId, client, secret) {
 *
 * @internal
 */
-async function resolveSubjectIdentifier(userId, client, opts) {
-	if (client.subjectType === "pairwise" && opts.pairwiseSecret) return computePairwiseSub(userId, client, opts.pairwiseSecret);
-	return userId;
+async function resolveSubjectIdentifier(userId, client, opts, use) {
+	const defaultSubject = client.subjectType === "pairwise" && opts.pairwiseSecret ? await computePairwiseSub(userId, client, opts.pairwiseSecret) : userId;
+	if (!opts.resolveSubjectIdentifier) return defaultSubject;
+	return opts.resolveSubjectIdentifier({
+		userId,
+		clientId: client.clientId,
+		subjectType: client.subjectType,
+		use,
+		defaultSubject
+	});
 }
 /**
 * Converts URLSearchParams to a plain object, preserving
